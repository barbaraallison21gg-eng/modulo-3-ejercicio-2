# 🛠️ E2-M3: Gestor Interactivo de Lista de Tareas (JavaScript)

Este proyecto es la solución al Ejercicio **E2-M3**, cuyo objetivo es aplicar conceptos fundamentales de JavaScript como **arrays**, **bucles (`while`, `for`)** y la **interacción con el usuario** a través de la consola/navegador.

---

## 🎯 Objetivo del Proyecto

Crear una aplicación de consola (ejecutable en el navegador a través del entorno de desarrollo o directamente en la consola) que permita al usuario:

1.  Añadir múltiples tareas de forma consecutiva.
2.  Mostrar la lista final de tareas de forma ordenada y numerada.

## ✨ Conceptos Clave Aplicados

| Concepto | Uso en el Código | Descripción |
| :--- | :--- | :--- |
| **Estructuras de datos** | `let listaDeTareas = [];` | Declaración de un **Array** para almacenar las tareas. |
| **Bucles** | `while (...)` | Utilizado para permitir la **entrada de datos interactiva** y continua. |
| **Métodos de Array** | `listaDeTareas.push(...)` | Añade la tarea ingresada al final de la lista. |
| **Entrada de Datos** | `prompt()`, `confirm()` | Obtiene la tarea del usuario y confirma si desea añadir más. |
| **Salida de Datos** | `console.log()` | Muestra el listado final de tareas en la consola. |
| **Bucles (Iteración)** | `for (let i = 0; ...)` | Recorre el array para generar la salida numerada. |

## 💻 Instrucciones de Uso (Ejecución)

1.  **Copia el código** provisto en un archivo `.js`.
2.  **Abre tu navegador** (Chrome, Firefox, etc.).
3.  Abre la **Consola de Desarrollador** (F12 o Clic derecho -> Inspeccionar).
4.  Navega a la pestaña "**Console**".
5.  **Pega el código** completo y presiona `Enter`.

El programa se iniciará inmediatamente, pidiéndote ingresar tareas a través de las ventanas `prompt()` y `confirm()` del navegador. Una vez que decidas terminar, el resultado final se imprimirá en la consola.

## 🔗 Enlaces

[Enlace al código fuente (`.js/main.js`)](.js/main.js)

